# Beta design-system kit

The maintained, bundled kit is in [the layout skill example](../../skills/generateblocks-layouts/examples/beta-design-system/README.md). It includes two design-system presets, a native editor builder, rendered block exports, reusable sections, and a localhost-only setup.
